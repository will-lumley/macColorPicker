use_frameworks!

target 'macColorPicker_Example' do
  platform :macos, '10.12'
  pod 'macColorPicker', :path => '../'

  target 'macColorPicker_Tests' do
    inherit! :search_paths
    pod 'macColorPicker', :path => '../'
  end
end
